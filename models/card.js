class Card {
  constructor(id, title, level, description, imageUrl) {
    this.id = id;
    this.title = title;
    this.level = level;
    this.description = description;
    this.imageUrl = imageUrl;
  }
}

export default Card;
